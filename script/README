# Introduction

This directory contains a series of scripts to compile this library in different platforms.

Using a Makefile script by giving the value of a command line argument---CMD, to choose which kind of language script is used to complate the task.