int main(int argc, char *argv[]) {
  fprintf(stdout, "name : %s\n", STRING(TYPE));
  fprintf(stdout, "size : %ld\n", sizeof(TYPE));
#ifndef TYPE_BOOL
  fprintf(stdout, "%d : " OUT_FORMAT "\n", 4, FUNCTION(NAME, usqrt)(4));
  fprintf(stdout, OUT_FORMAT " : " OUT_FORMAT "\n", MAX,
          FUNCTION(NAME, usqrt)(MAX));
  fprintf(stdout, OUT_FORMAT "\n", FUNCTION(NAME, mul)(-64, 64));
  fprintf(stdout, OUT_FORMAT "\n", FUNCTION(NAME, ufact)(4));
#endif

  return 0;
}