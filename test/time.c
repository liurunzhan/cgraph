#include <stdio.h>

#include "cgraph_base.h"

#include "cgraph_time.h"

int main(int argc, char *argv[]) {
  cgraph_file_fprintfln(stdout, "check matrix apis");
  FUNCTION(NAME, details)();
  FUNCTION(NAME, fprintln1)
  (stdout, FUNCTION(NAME, localtime1)());

  return 0;
}
