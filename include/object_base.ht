#include "data_base.ht"

extern cgraph_type_t FUNCTION(NAME, objtype)(const TYPE *cthis);
extern cgraph_bool_t FUNCTION(NAME, objaccess)(const TYPE *cthis);
extern cgraph_bool_t FUNCTION(NAME, objhash)(const TYPE *cthis);

extern TYPE *FUNCTION(NAME, chgtype)(TYPE *cthis, const cgraph_type_t type,
                                     const cgraph_size_t size);
extern TYPE *FUNCTION(NAME, setaccess)(TYPE *cthis);
extern TYPE *FUNCTION(NAME, clraccess)(TYPE *cthis);
