/**
 * @file cgraph_template_matrix.ht
 * @brief the common apis of matrix
 * @author liurunzhan
 * @email liurunzhan@sina.com
 * @package cgraph
 * @version 0.0.0
 * @date 2021-03-31
 * @lisence GPL-3.0
 * @url https://github.com/liurunzhan/cgraph
 */

#include "cgraph_template_struct.ht"

extern TYPE *FUNCTION(NAME, create)(const cgraph_type_t type,
                                    const cgraph_size_t row,
                                    const cgraph_size_t column);
extern TYPE *FUNCTION(NAME, retype)(TYPE *cthis, const cgraph_type_t type);
extern TYPE *FUNCTION(NAME, resize)(TYPE *cthis, const cgraph_type_t type,
                                    const cgraph_size_t row,
                                    const cgraph_size_t column);
extern cgraph_size_t FUNCTION(NAME, index_row)(const TYPE *cthis);
extern cgraph_size_t FUNCTION(NAME, index_column)(const TYPE *cthis);
extern cgraph_size_t FUNCTION(NAME, size_rowcol)(const TYPE *cthis);
extern cgraph_bool_t FUNCTION(NAME, issquare)(const TYPE *cthis);
extern cgraph_bool_t FUNCTION(NAME, ismatrix_row)(const TYPE *cthis);
extern cgraph_bool_t FUNCTION(NAME, ismatrix_column)(const TYPE *cthis);
extern cgraph_bool_t FUNCTION(NAME, ismatrix)(const TYPE *cthis);
