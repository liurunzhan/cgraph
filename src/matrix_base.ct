cgraph_size_t FUNCTION(NAME, row)(const TYPE *cthis)
{
  return (NULL != cthis ? cthis->row : 0);
}

cgraph_size_t FUNCTION(NAME, column)(const TYPE *cthis)
{
  return (NULL != cthis ? cthis->column : 0);
}
